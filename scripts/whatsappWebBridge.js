"use strict";

// Backward-compatible entrypoint. The WhatsApp Web testing adapter now lives
// in its own package folder so it stays separate from real API-channel code.
require("../whatsapp-web-test-bridge/src/whatsappWebBridge");
